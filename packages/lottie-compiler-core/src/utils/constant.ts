enum LayerTypeEnum {
    'precomp' = 0,
    'solid' = 1,
    'image' = 2,
    'null' = 3,
    'shape' = 4,
    'text' = 5,
}

const TypeEnum = ['precomp', 'solid', 'image', 'null', 'shape', 'text'];

export {
    LayerTypeEnum,
    TypeEnum,
}